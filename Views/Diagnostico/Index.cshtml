@model IEnumerable<Clinica.Models.Diagnostico>

@{
    ViewData["Title"] = "Diagnostico";
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdConsultaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdConsultaNavigation.IdConsulta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDiagnostico">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDiagnostico">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDiagnostico">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
